name: Container Images

on:
  pull_request:
    paths:
      - ".github/workflows/container-images.yml"
      - "images/**"
      - "scripts/**"
      - "README.md"
  push:
    branches: [main]
    tags: ["v*"]
    paths:
      - ".github/workflows/container-images.yml"
      - "images/**"
      - "scripts/**"
      - "README.md"
  workflow_dispatch:
    inputs:
      unilink_ref:
        description: "unilink tag, branch, or commit to install"
        required: false
        default: "v0.7.2"

permissions:
  contents: read
  packages: write

env:
  DEFAULT_UNILINK_REF: v0.7.2
  IMAGE_NAME: ghcr.io/unilink-lab/unilink-core

jobs:
  core:
    name: Build unilink-core
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v6

      - name: Resolve unilink ref
        id: ref
        shell: bash
        run: |
          if [[ "${GITHUB_REF_TYPE}" == "tag" ]]; then
            echo "value=${GITHUB_REF_NAME}" >> "${GITHUB_OUTPUT}"
          elif [[ "${GITHUB_EVENT_NAME}" == "workflow_dispatch" ]]; then
            echo "value=${{ inputs.unilink_ref }}" >> "${GITHUB_OUTPUT}"
          else
            echo "value=${DEFAULT_UNILINK_REF}" >> "${GITHUB_OUTPUT}"
          fi

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v4

      - name: Log in to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v4
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract metadata
        id: meta
        uses: docker/metadata-action@v6
        with:
          images: ${{ env.IMAGE_NAME }}
          tags: |
            type=raw,value=latest,enable={{is_default_branch}}
            type=ref,event=tag
            type=sha,prefix=sha-

      - name: Build image
        uses: docker/build-push-action@v7
        with:
          context: .
          file: images/core/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          build-args: |
            UNILINK_REF=${{ steps.ref.outputs.value }}
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max
